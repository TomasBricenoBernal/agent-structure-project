# QA

- `review_depth`: `Light`
- `status`: `Done`

## Checks

- Confirm `AGENTS.md` and `CLAUDE.md` stay synchronized in all shared sections.
- Confirm each agent file has a clear limitation.
- Confirm the new `DevOps` specialization matches the shared protocol.

## Findings

- `AGENTS.md` and `CLAUDE.md` remain aligned in the shared protocol sections.
- All agent specialization files now include an explicit limitation.
- `DevOps` now has a dedicated specialization file and a tighter activation boundary.
