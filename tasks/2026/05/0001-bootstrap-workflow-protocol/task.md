# Task

- `task_id`: `0001-bootstrap-workflow-protocol`
- `title`: `Bootstrap workflow protocol`
- `status`: `Done`
- `related_feature`: `0001-workflow-protocol-foundation`
- `related_feature_path`: `features/0001-workflow-protocol-foundation/`
- `owner`: `Codex`
- `created_on`: `2026-05-05`
- `next_step`: `Create the next active task before new non-trivial work starts.`

## Goal

Create the initial shared workflow system for Codex and Claude, including tracking, review, and agent-role structure.

## Scope

- In scope: protocol definitions, templates, agent-role files, and bootstrap references.
- Out of scope: application feature development.

## Acceptance Criteria

- Shared protocol exists in `AGENTS.md` and `CLAUDE.md`.
- Claude role files exist in `.claude/agents/`.
- Task and feature templates exist and are usable.
- Documentation index clarifies where reusable repository references belong.

## Notes

- This task acts as the reference example for future task tracking.
